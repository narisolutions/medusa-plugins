{
  "name": "medusa-plugins",
  "private": true,
  "workspaces": [
    "./plugins/*",
    "./packages/*"
  ],
  "scripts": {
    "build": "yarn workspaces run build",
    "test": "yarn workspaces run test"
  },
  "engines": {
    "node": ">=20"
  }
}
